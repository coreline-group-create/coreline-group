import React from "react";
import ReactDOM from "react-dom/client";
import CorelineGroupFinalWebsite from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CorelineGroupFinalWebsite />
  </React.StrictMode>
);
